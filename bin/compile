#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

# fail fast
set -e

echo "-----> Compile Step, exporting QMAKE, QT_SELECT, test install"
export QMAKE=/usr/bin/qmake
export QT_SELECT=qt5
echo "-----> QMAKE=$QMAKE"
